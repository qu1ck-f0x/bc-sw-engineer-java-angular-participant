# Lab 14 — Entity vs DTO

Entity = persistence shape;
DTO = data transfer object

Leak risks:

1. Internal flags or audit columns in responses
2. Sensitive data exposure

Amina response fields: customerId, fullName, status
